<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/fragment_messenger_item_user_image"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_margin="16dp"/>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_toEndOf="@id/fragment_messenger_item_user_image">

        <TextView
            android:id="@+id/fragment_messenger_item_user_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/activity_login_picture_user_image"
            android:textSize="16sp"
            android:layout_marginBottom="4dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/text_color_primary"
            android:fontFamily="sans-serif"/>

        <TextView
            android:id="@+id/fragment_messenger_item_message_preview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/activity_login_picture_user_image"
            android:textSize="14sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/text_color_secondary"
            android:fontFamily="sans-serif"/>

    </LinearLayout>

</RelativeLayout>